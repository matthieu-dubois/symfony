{# empty Twig template #}
{% extends "EpsiPremierBundle::layout.html.twig" %}
{% block title %} {{ parent() }} - Admin {% endblock %}    
{% block contenu %}  <br />Liste des thèmes {% if not listeTheme is empty %}
<table>
    <tr><th>id</th><th>Libelle</th></tr>   
    {% for key,value in listeTheme %}  
        <tr><td>{{ value.id }}</td><td>{{ value.Libelle }}</td>
        <td>
        <a href="{{ path('epsi_premier_modification_theme',{'id':value.id})}}">
            Modifier</a>
    </td>
    <td>
        <a href="{{ path('epsi_premier_suppression_theme',{'id':value.id})}}">
            Supprimer</a>
    </td>
        </tr>    
    {% endfor %}  </table>    
 {% else %}   
     Aucuns thèmes à afficher   
     {% endif %}  
         {% endblock %} 