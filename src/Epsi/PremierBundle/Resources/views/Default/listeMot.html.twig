{# empty Twig template #}
{% extends "EpsiPremierBundle::layout.html.twig" %}
{% block title %} {{ parent() }} -admin {% endblock %}    
{% block contenu %}   <br />Liste des Categories {% if not ListeMot is empty %}
         
<table>
    <tr><th>Mots Anglais</th><th>Mots Français </th></tr>  
    
    
    {% for key,value in ListeMot %}
        
        <tr><td>{{ value.Libelle }}</td><td>{{ value.Traduction }}</td><
          
          
    <td>
        <a href="{{ path('epsi_premier_modification_mot',{'id':value.id})}}">
            Modifier</a>
    </td>
    
    <td>
        <a href="{{ path('epsi_premier_suppression_mot',{'id':value.id})}}">
            Supprimer</a>
    </td>
     
        </tr>  
    {% endfor %} 
</table>   

 {% else %}   
     Aucun mots à afficher   
     {% endif %}  
         {% endblock %}
         
         