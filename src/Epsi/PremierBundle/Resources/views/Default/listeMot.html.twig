{# empty Twig template #}
{% extends "EpsiPremierBundle::layout.html.twig" %}
{% block title %} {{ parent() }} - Admin {% endblock %}    
{% block contenu %}  <br />Liste des Mot {% if not ListeMot is empty %}
<table>
    <tr><th>id</th><th>Libelle</th><th>Traduction</th></tr>  
    {% for key,value in ListeMot %}  
        <tr><td>{{ value.id }}</td><td>{{ value.Libelle }}</td><td>{{ value.Traduction }}</td>
            
            <td>
        <a href="{{ path('epsi_premier_modification_mot',{'id':value.id})}}">
            Modifier</a>
    </td>
    
    <td>
        <a href="{{ path('epsi_premier_suppression_mot',{'id':value.id})}}">
            Supprimer</a>
    </td>
     
        </tr>  
    {% endfor %}
    
</table>    
 {% else %}   
     Aucun mot à afficher   
     {% endif %}  
         {% endblock %}